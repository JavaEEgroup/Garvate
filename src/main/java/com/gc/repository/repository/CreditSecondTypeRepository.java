package com.gc.repository.repository;

import com.gc.model.CreditSecondType;
import org.springframework.data.jpa.repository.JpaRepository;


public interface CreditSecondTypeRepository extends JpaRepository<CreditSecondType,Long> {
}
