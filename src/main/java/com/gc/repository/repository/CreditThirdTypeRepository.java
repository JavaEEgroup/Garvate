package com.gc.repository.repository;

import com.gc.model.CreditThirdType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CreditThirdTypeRepository extends JpaRepository<CreditThirdType,Long> {
}
